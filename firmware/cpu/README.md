# CPU firmware

This directory will contain the ESP-IDF implementation of the Z80-compatible
CPU module, memory, SD-backed disks, console, and SPI-host transport.

The guest-visible contract is [the CPU v0.1 specification](../../docs/specifications/cpu-v0.1.md).
